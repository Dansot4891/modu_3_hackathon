import 'package:flutter/material.dart';

class PhotoInfoPage extends StatelessWidget {
  final int id;
  const PhotoInfoPage({required this.id, super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
